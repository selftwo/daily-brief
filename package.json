{
  "name": "daily-brief",
  "version": "0.1.0",
  "private": false,
  "description": "A public, source-grounded newspaper of signals, papers, essays, podcasts, and field notes.",
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev",
    "build": "python3 pipeline/public_safety.py && python3 pipeline/editorial_lint.py && python3 pipeline/sourcebook_smoke.py && ASTRO_TELEMETRY_DISABLED=1 astro build",
    "preview": "astro preview",
    "check": "npm test && npm run build",
    "lint:public": "python3 pipeline/public_safety.py",
    "lint:copy": "python3 pipeline/editorial_lint.py",
    "test": "python3 tests/test_public.py && python3 tests/test_intake.py",
    "ingest:public": "python3 pipeline/collect_public.py"
  },
  "dependencies": {
    "astro": "^7.0.7"
  }
}
